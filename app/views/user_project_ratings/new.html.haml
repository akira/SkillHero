.block
  .secondary-navigation
    %ul.wat-cf
  .content
    %h2.title
      = t("web-app-theme.new", :default => "New")
      UserProjectRating
    .inner
      = form_for :user_project_rating, :url => user_project_ratings_path, :html => { :class => :form } do |f|
        = render :partial => "form", :locals => {:f => f}
- content_for :sidebar, render(:partial => 'sidebar')

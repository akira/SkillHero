.block
  .secondary-navigation
    %ul.wat-cf
      %li.first= link_to "#{t("web-app-theme.list", :default => "List")}", non_profits_path
      %li.active= link_to "#{t("web-app-theme.new", :default => "New")}", new_non_profit_path
  .content
    %h2.title
      = t("web-app-theme.new", :default => "New")
      NonProfit
    .inner
      = form_for :non_profit, :url => non_profits_path, :html => { :class => :form } do |f|
        = render :partial => "form", :locals => {:f => f}
- content_for :sidebar, render(:partial => 'sidebar')
